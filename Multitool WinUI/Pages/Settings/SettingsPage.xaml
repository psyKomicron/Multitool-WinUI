<Page
    x:Class="MultitoolWinUI.Pages.Settings.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultitoolWinUI.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MaxWidth="300"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="30" Height="40" MaxHeight="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <AutoSuggestBox
            x:Name="SettingSearch"
            Grid.Row="0"
            Grid.Column="0"/>
        <ListView
            x:Name="PagesNavigationView"
            ItemClick="PagesNavigationView_ItemClick"
            IsItemClickEnabled="True"
            Grid.Row="1"
            Grid.Column="0">
            <ListViewItem>
                <TextBlock Text="Main window" Tag="MainPage"/>
            </ListViewItem>

            <ListViewItem>
                <TextBlock Text="Explorer" Tag="ExplorerPage"/>
            </ListViewItem>

            <ListViewItem>
                <TextBlock Text="Control panels" Tag="ControlPanelsPage"/>
            </ListViewItem>

            <ListViewItem>
                <TextBlock Text="Power controls" Tag="PowerPage"/>
            </ListViewItem>

            <ListViewItem>
                <TextBlock Text="General" Tag="General"/>
            </ListViewItem>
        </ListView>

        <Frame 
            x:Name="ContentFrame"
            CornerRadius="7"
            Grid.Column="1"
            Grid.Row="1"/>
    </Grid>
</Page>
