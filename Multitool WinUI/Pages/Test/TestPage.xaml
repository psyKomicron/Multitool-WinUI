<Page
    x:Class="MultitoolWinUI.Pages.Test.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultitoolWinUI.Pages.Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:twitch="using:Multitool.Net.Twitch"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox
            x:Name="ChatInput"
            PlaceholderText="Enter message"
            TextWrapping="Wrap"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            AcceptsReturn="True"
            ScrollViewer.VerticalScrollMode="Auto"
            Grid.Column="0"/>

        <Button Grid.Column="1" Click="Button_Click" Content="☑️"/>

        <TextBlock
            x:Name="Result"
            IsTextSelectionEnabled="True"
            Grid.Column="2"
            TextWrapping="Wrap"/>
    </Grid>
</Page>
