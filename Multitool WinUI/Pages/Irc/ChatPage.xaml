<Page
    x:Class="MultitoolWinUI.Pages.Irc.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:MultitoolWinUI.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView
            ItemsSource="{x:Bind Chat}"
            SelectionMode="None">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel
                        VerticalAlignment="Bottom"
                        ItemsUpdatingScrollMode="KeepLastItemInView"
                        CacheLength="200"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ChatMessageModel">
                    <Grid Width="Auto" ColumnSpacing="7">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35px"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock 
                            Text="{Binding Timestamp, Mode=OneWay}"
                            Opacity="0.5"
                            Grid.Column="0"
                            Grid.Row="0"/>

                        <TextBlock 
                            Text="{Binding UserName, Mode=OneWay}"
                            IsTextSelectionEnabled="True"
                            TextDecorations="Underline"
                            Foreground="{Binding NameColor, Mode=OneWay}"
                            TextWrapping="Wrap"
                            Grid.Column="1"
                            Grid.Row="0"/>

                        <TextBlock 
                            Text="{Binding Message, Mode=OneWay}"
                            IsTextSelectionEnabled="True"
                            TextWrapping="WrapWholeWords"
                            HorizontalAlignment="Stretch"
                            Grid.ColumnSpan="2"
                            Grid.Column="1"
                            Grid.Row="1"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
