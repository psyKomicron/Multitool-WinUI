<UserControl
    x:Class="MultitoolWinUI.Controls.ImageTester"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultitoolWinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ProgressRing x:Name="DownloadProgress" Visibility="Collapsed" Grid.Column="0" Grid.RowSpan="2"/>

        <TextBox
            x:Name="SourceInputTextBox" 
            InputScope="Url" 
            AcceptsReturn="True" 
            PreviewKeyDown="SourceInputTextBox_PreviewKeyDown" 
            Width="500"
            HorizontalAlignment="Center" 
            VerticalAlignment="Top"
            Grid.Column="1"
            Grid.Row="0"/>
        <Image 
            x:Name="TestImage" 
            Grid.Column="1"
            Grid.Row="1"/>
    </Grid>
</UserControl>
